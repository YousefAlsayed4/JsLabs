window.alert("Show a message to tell the user that the first release of a calculator that only has a summation feature. ")

var Fnum= window.prompt("Enter the first Number");
var Snum= window.prompt("Enter the second Number");
var sum = Number(Fnum)+Number(Snum);

window.alert(`Result ${sum}`);
